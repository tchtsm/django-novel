from django.shortcuts import render

# Create your views here.
def login(request, name, pwd, remenber):
    if request.method == post:
        try:
            name =
            pwd =
        except:
            pass
    else:
        return

def register(request):
    if request.method==get:
        pass
    elif request.method==post:
        pass
    else:
        return